# Dev/local-only overrides. Loaded when SPRING_PROFILES_ACTIVE=dev (or local).
# Surfaces error messages + stacktraces over HTTP so the UI shows actionable
# failure detail during development. Never enable this in shared environments.
server.error.include-message=always
server.error.include-stacktrace=always
